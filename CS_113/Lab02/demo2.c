#include <stdio.h>
#include <stdlib.h>

int main()
{
    // relational operator : > <  == -> 0, 1
    // logical : && || !

    char str[20];
    // fgets(str, 20, stdin);

    // double score = atof(str);

    // if (score >= 90)
    // {
    //     printf("A");
    // }

    // int i = 0;
    // while ()
    // {

    // }

    for (int j = 0; j < 20; j++) // for (intial; contition check before; after loop )
    {
        }

    return 0;
}
